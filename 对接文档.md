# 易支付虚拟商品插件 — 对接文档

插件 ID：`epay_shop`  
名称：易支付虚拟商品（决定数据目录，勿随意改）  
产物 DLL：`PayBot.dll`  
适用：GBot + QQ 官方机器人 + 易支付（V1 接口）

按群隔离开店：下单 → 支付 → 回调/轮询 → 自动发卡密。

---

## 1. 部署

1. 编译（仓库根目录双击或命令行）：

```bat
build.bat
```

默认 `Release`，产物复制到 `D:\GBOT\plugin\PayBot.dll`。

2. 若 GBot 正在运行：重载插件或重启进程。
3. 确认插件目录有 `System.Text.Encoding.CodePages.dll`（TXT 编码用；`build.bat` 会尽量一并复制）。

可选：`build.bat Debug` 输出到 `bin\Debug`。

---

## 2. 全局设置（插件设置窗）

在 GBot 中打开本插件设置，填写：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| RobotId | 官方机器人 AppID，主动发群/私聊消息用 | `102766364` |
| MasterId | 最高权限 openid，逗号可多人 | `OPENID1,OPENID2` |
| HttpPort | 本地接收易支付回调的端口 | `8087` |
| NotifyBase | **公网**回调基址（不要末尾 `/`） | `https://pay.example.com` 或 `http://1.2.3.4:8087` |
| NotifyAdmin | 发货成功后是否私聊通知管理员/店主 | 建议开启 |

下单时拼出的异步通知地址：

```text
{NotifyBase}/epay/notify
```

若 `NotifyBase` 为空，会退化成：

```text
http://127.0.0.1:{HttpPort}/epay/notify
```

公网易支付**无法**访问 `127.0.0.1`，必须配置可达的 `NotifyBase`（域名反代或公网 IP + 端口映射）。

### Windows 端口监听说明

插件用 `HttpListener` 监听 `http://+:{端口}/`。若无 URLACL，会降级为仅 `127.0.0.1`，日志会出现「无 URLACL」。

管理员 CMD 可授权（端口改成你的）：

```bat
netsh http add urlacl url=http://+:8087/ user=Everyone
```

---

## 3. 开店流程（推荐顺序）

```text
① 群内：群信息          ← 获取群ID / 自己的 openid
② 私聊：存主人<群ID>
③ 私聊：设置易支付 群ID#网关#pid#密钥
④ 群内：开启易支付
⑤ 群内：添加商品 名称|价格|描述
⑥ 私聊上架卡密（文本或 TXT）
⑦ 买家：购买 <商品ID> → 支付 → 查单
```

群 ID、用户 openid 以机器人消息上下文为准（一般是官方 openid 字符串）。  
群内发送 `群信息`（别名：`本群信息` / `群ID`）即可查看，**未开启易支付时也能用**。

---

## 4. 权限

| 角色 | 判定 | 能力 |
|------|------|------|
| 最高权限 | 全局 `MasterId` | 删主人、等同各群店主 |
| 群店主 | `存主人` 绑定，或店铺内 `owner` | 开店配置、上下架、补单、库存 |
| 普通用户 | 其他 | 商品、购买、查单、我的订单 |

店主相关命令须**私聊**机器人；群内发会提示去私聊。

---

## 5. 命令一览

### 5.1 私聊（须带群 ID）

| 命令 | 格式 | 说明 |
|------|------|------|
| 存主人 | `存主人<群ID>` | 绑定当前账号为该群店主（已有店主不可覆盖） |
| 查主人 / 群主人 | `查主人 <群ID>` | 查询店主 |
| 删主人 | `删主人 <群ID>` | 仅最高权限 |
| 设置易支付 | `设置易支付 群ID#网关#pid#密钥` | 一次写好四项（仍兼容旧写法） |
| 设置网关 | `设置网关 <群ID> <网址>` | 如 `https://pay.xxx.cn` |
| 设置商户 | `设置商户 <群ID> pid\|密钥` | |
| 设置支付方式 | `设置支付方式 <群ID> 支付宝/微信/QQ` | |
| 设置跳转 | `设置跳转 <群ID> <网址>` | 支付完成跳转，可空 |
| 设置站点 | `设置站点 <群ID> <名称>` | 易支付站点名 |
| 设置IP | `设置IP <群ID> <IP>` | 下单 clientip |
| 设置前缀 | `设置前缀 <群ID> <前缀>` | 群命令前缀；`空/无/清除` 可清空 |
| 上架卡密 | 见下文 | 文本或 TXT |
| 店铺配置 / 查看配置 | `店铺配置 <群ID>` | 查看本店支付配置与回调地址 |
| 帮助 / 菜单 | `帮助` | |

设置易支付示例：

```text
设置易支付 670EA1E621C22A8BBE75F8BA0BD9C969#https://pay.10sn.cn#1000#你的密钥
```

### 5.2 群内

可配置命令前缀（`设置前缀`）。下列以无前缀为例。

| 命令 | 格式 | 谁可用 |
|------|------|--------|
| 群信息 | `群信息` / `本群信息` / `群ID` | 所有人（未开店也可用） |
| 菜单 / 帮助 | `菜单` | 所有人 |
| 商品 | `商品` | 所有人 |
| 购买 / 买 | `购买1001` / `买 1001` | 所有人 |
| 查单 | `查单` / `查单 <订单号>` | 所有人（卡密仅买家可见） |
| 我的订单 | `我的订单` | 所有人 |
| 我的统计 | `我的统计` / `下单统计` | 所有人（仅统计交易成功） |
| 开启易支付 / 开易支付 | | 店主 |
| 关闭易支付 / 关易支付 | | 店主（默认关闭） |
| 添加商品 | `添加商品 名称\|价格\|描述` | 店主 |
| 删除商品 | `删除商品 <商品ID>` | 店主 |
| 库存 / 库存查询 | `库存` | 店主（只显示数量，不回显明文卡密） |
| 统计 | `统计` / `销售统计` | 店主（成功笔数/金额/用户排行，排行显示昵称） |
| 补单 | `补单 <订单号>` | 店主（强制按本地订单发货） |

添加商品示例：

```text
添加商品 月卡|9.9|一个月会员
```

成功后会得到商品 ID（如 `1001`），并提示私聊上架卡密。

---

## 6. 上架卡密

### 6.1 文本方式（私聊）

```text
上架卡密 <群ID> <商品ID>
卡密1
卡密2
卡密3
```

也支持同一行用中英文逗号分隔：

```text
上架卡密 <群ID> 1001 KEY1,KEY2,KEY3
```

说明：

- 私聊会保留换行，一行一个卡密即可。
- 自动去重：批次内重复、已在库中的会跳过。
- 单次最多 **2000** 张（去重后）；更多请拆分多次发送。

### 6.2 TXT 文件方式（私聊）

文件名（用 `#` 分隔）：

```text
群ID#商品ID.txt
```

示例：

```text
670EA1E621C22A8BBE75F8BA0BD9C969#1001.txt
```

文件内容：一行一个卡密。编码支持 UTF-8 / 带 BOM / GB18030（常见记事本 ANSI）。

将 TXT **私聊发送给机器人**（不要发群）。需为对应群店主。

安全限制：

- 仅允许 `http`/`https`，禁止内网/本机地址，禁止跟随危险重定向。
- 文件大小上限约 **2MB**。

---

## 7. 易支付对接

### 7.1 下单

插件调用商户网关的创建接口（`mapi.php` / 兼容逻辑见 `EpayClient`），参数含：

- `pid` / `type` / `out_trade_no` / `notify_url` / `return_url` / `name` / `money` / `clientip` / `sign` 等  
- 签名：参数按键名排序拼接 + 商户密钥，MD5（小写 hex）

支付方式映射：支付宝 / 微信 / QQ（以店铺「设置支付方式」为准，下单时可选手动指定）。

### 7.2 异步回调

- 路径：`POST/GET /epay/notify`
- 成功时插件响应纯文本：`success`（易支付要求）
- **必须带正确 `sign`**；无签名或验签失败一律拒绝发货（防伪造回调）

回调可达性：

1. 设置公网 `NotifyBase`
2. 反代或端口映射到本机 `HttpPort`
3. 防火墙放行

### 7.3 查单轮询

插件每约 **1 分钟**轮询待支付订单（HTTP 次数有上限），作为回调的补充。  
订单支付超时默认 **2 分钟**，超时关闭后短时仍可能监听到「迟到支付」并补发。

---

## 8. 订单与发货

| 状态 | 含义 |
|------|------|
| pending | 待支付 |
| paid | 已支付（中间态） |
| delivered | 已发货 |
| no_stock | 已支付但缺货 |
| closed | 已关闭（如超时） |

发货规则：

- 库存 FIFO（先进先出）。
- 扣库存与标已发货在同一把锁内完成，降低并发双发风险。
- 卡密优先以「仅购买者可点」的键盘按钮展示；失败则降级文案提示买家本人查单。
- 缺货会通知店主；补货后需买家「查单」或店主「补单」。

补单：店主群内 `补单 <订单号>`，不二次问易支付是否已付，请谨慎使用。

---

## 9. 数据文件

数据目录由 GBot 分配（插件 `GetDataDir()`），大致结构：

```text
{dataDir}/
  group_owners.json          # 群 → 店主 openid
  shops/
    {群ID}.json              # 店铺配置、商品元数据、Pending、序号
    {群ID}.stock.json        # 各商品卡密库存
    {群ID}.orders.json       # 订单
```

全局配置路径见设置窗底部提示（`GetConfigPath()`）。

说明：

- 首次加载旧版「单体」`{群ID}.json` 会自动拆成上述三份文件。
- 主 JSON 损坏时**拒绝覆盖为空店**，避免库存被清空；请从备份恢复。
- 卡密与商户密钥以明文落在数据目录，请做好目录权限与备份加密。

---

## 10. 买家操作示例

1. 群内发：`商品`
2. 发：`购买1001`，按提示选支付方式并打开链接付款（约 2 分钟内）
3. 付款后发：`查单` 或 `查单 <订单号>`
4. 点击「卡密」按钮查看（**不要点发送**），自行复制保存

---

## 11. 常见问题

| 现象 | 排查 |
|------|------|
| 付了钱不自动发货 | 查 `NotifyBase` 是否公网可达；看日志有无回调；等 1 分钟轮询；店主可 `补单` |
| 回调验签失败 | 密钥是否与易支付后台一致；是否被反代改写了 body/query |
| 仅监听 127.0.0.1 | 配置 URLACL 或改用反代打到 127.0.0.1:端口 |
| 本群商店已关闭支付 | 群内店主发 `开启易支付` |
| TXT 发了没反应 | 文件名是否为 `群ID#商品ID.txt`；是否私聊；是否店主；重载插件 |
| 上架显示 0 张新卡密 | 与库内完全重复会被跳过 |
| 店铺数据损坏提示 | 检查 `shops` 下对应 json 是否半截写入，用备份覆盖 |

---

## 12. 安全要点（对接时注意）

1. 回调**强制验签**，不要对公网暴露无鉴权的伪造 notify。  
2. 最高权限 `MasterId`、数据目录仅管理员可读写。  
3. TXT 下载禁止内网地址，勿把机器人私聊当任意文件下载器。  
4. `补单` 可直接吐库存，仅店主可用，账号被盗风险需自控。

---

## 13. 版本与编译

- 目标框架：`net8.0`
- 依赖：`GBot.PluginAbstractions`（宿主提供）、Avalonia（设置窗）、`System.Text.Encoding.CodePages`
- 编译：`build.bat` → `D:\GBOT\plugin\`

如需改部署目录，编辑 `build.bat` 中的 `DEPLOY_DIR`。

---

## 14. 自更新（PayBot.dll）

从公开仓 `TenYearsPro/PayBotdll` 的 GitHub Release 检查/下载更新。

### 用法

- 群内店主或最高权限：`检查更新` / `更新插件`
- 私聊最高权限：`检查更新`
- 插件启用约 45 秒后会自动检查一次（可关）

有新版本时会下载为 `plugin\PayBot.dll.new`，并启动后台替换；**请完全退出再打开 GBot**，退出后自动覆盖 `PayBot.dll`。

### 全局配置（`易支付虚拟商品.json`）

| 字段 | 默认 | 说明 |
|------|------|------|
| update_owner | TenYearsPro | 仓库 Owner |
| update_repo | PayBotdll | 仓库名 |
| update_asset | PayBot.dll | Release 附件名 |
| update_auto_check | true | 启动自动检查 |
| update_include_prerelease | false | 是否接受预发布 |
| github_token | 空 | 可选，提高 API 限额 |
| update_proxies | gh-proxy.com / ghproxy.net | GitHub 加速前缀（优先于直连） |

---

